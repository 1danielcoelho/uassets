{
  "name": "uassets",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun dev.ts",
    "build": "bun build index.html --outdir dist --minify && cp -r examples dist/examples",
    "test": "bun test .test",
    "dump": "bun run src/cli/dump.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
